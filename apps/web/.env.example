# Public origin for the deployed @webrex/lab Cloudflare Worker.
# Required for production so embedded lesson demos do not point at localhost.
PUBLIC_WEBREX_LAB_ORIGIN=https://your-webrex-lab.example.com

# Optional site URL used by auth redirects and metadata.
# PUBLIC_SITE_URL=https://your-webrex-site.example.com
